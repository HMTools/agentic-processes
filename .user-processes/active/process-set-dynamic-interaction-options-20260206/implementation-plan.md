# Step 4: Implementation Plan

Ordered execution plan for the "Dynamic Interaction Options" concept. All changes are derived from `step3-analysis.md`.

**Total items**: 17 actionable changes across 6 phases
**Repos**: `agentic-processes` (Phases 1-4, 6), `agentic-processes-ui` (Phase 5)

---

## Phase 1: Framework Types (foundation changes)

These establish the new type structure that everything else depends on.

### Item 1 — `process-instance.ts`

**File**: `.processes/types/process-instance.ts`
**Action**: RESTRUCTURE

**1a. Remove from ProcessStep** (delete lines 140-145):
```typescript
  
    /** Structured options for user selection when step needs input */
    interactionOptions?: InteractionOption[];
  
    /** IDs of options selected by the user (populated after selection) */
    selectedOptions?: string[];
```

**1b. Add to ProcessInstance** (after line 64 `steps: ProcessStep[];`, before `subProcessState`):
```typescript
  
    /** Current pending user interaction, if any. Set by agent when input is needed, cleared when user responds. */
    pendingInteraction?: PendingInteraction;
```

**1c. Add PendingInteraction interface** (after `InteractionOption` interface, before `ParentProcessRef` around line 176):
```typescript

/**
 * Represents a pending user interaction at the process level.
 * Set by the agent when user input is needed, cleared when user responds.
 */
export interface PendingInteraction {
  /** Options for user selection, dynamically generated by the agent */
  options: InteractionOption[];
}
```

**1d. Update JSDoc example** (lines 147-161): Replace the example to show `pendingInteraction` on process level instead of `interactionOptions` on step.

---

### Item 2 — `template-definition.ts`

**File**: `.processes/types/template-definition.ts`
**Action**: REMOVE

Remove the entire `interactionOptions` field definition from the step type (lines 71-81):
```typescript
    /** Structured options for user selection when step needs input */
    interactionOptions?: Array<{
      /** Unique identifier for this option */
      id: string;
      /** Display label for this option */
      label: string;
      /** Optional description/tooltip */
      description?: string;
      /** Whether this is the default option */
      isDefault?: boolean;
    }>;
```

---

## Phase 2: Operating Principle (the concept itself)

This establishes the principle that all guidance and behavior references.

### Item 3 — `operating-principles.md`

**File**: `.processes/steps/_components/operating-principles.md`
**Action**: ADD

**3a. Update title** (line 5): Change `## The 7 Principles` to `## The 8 Principles`

**3b. Add Principle 8** (after line 54, end of file):
```markdown

---

### 8. GENERATE INTERACTION OPTIONS
**Rule**: When you need user input (approval, choices, decisions), dynamically generate relevant options and set them in `process.json` `pendingInteraction` field. Never use predefined options from templates.

**Verification**: Output "✓ pendingInteraction set in process.json" when options are generated
```

---

### Item 4 — `process-continue.md`

**File**: `.processes/prompts/process-continue.md`
**Action**: ADD + CLEAN UP

**4a. Add Principle 8 to inline principles list** (after line 48, after Principle 7):
```markdown

8. **GENERATE INTERACTION OPTIONS** - When you need user input (approval, choices, decisions), dynamically generate options and set `pendingInteraction` in process.json
   - Output: "✓ pendingInteraction set in process.json"
```

**4b. Clean up interactionOptions text** (line 177): Replace:
```
   - **Handle approval checkpoints**: If step has `approvalRequired: true`, the subagent will prepare deliverables and return; **ensure `interactionOptions` are set** on the step entry in `process.json` (if missing, add default `approve`/`request-changes` options) and present them to the user alongside the deliverables, then wait for approval
```
With:
```
   - **Handle approval checkpoints**: If step has `approvalRequired: true`, the subagent will prepare deliverables and return; present deliverables to the user and wait for approval
```

---

## Phase 3: Guidance Cleanup (remove scattered references)

These files had interactionOptions-specific text that is now replaced by Principle 8.

### Item 5 — `mandatory-approval-checkpoint.md`

**File**: `.processes/steps/_components/mandatory-approval-checkpoint.md`
**Action**: CLEAN UP — remove all interactionOptions-specific lines

Principle 8 already covers the interaction options concept. This file should focus purely on the approval checkpoint flow. Remove:

- **Line 7**: Remove the `interactionOptions` checklist item entirely
- **Line 8**: Remove the "Present the interactionOptions" checklist item entirely  
- **Line 11**: Remove the `selectedOptions` checklist item entirely
- **Lines 14-16**: Remove both paragraphs about interactionOptions placement and fallback behavior
- **Line 26**: Remove the `[Present interactionOptions from process.json here]` placeholder

Keep everything else (the approval flow: present deliverables, wait, handle approval/rejection).

---

### Item 6 — `process-new.md`

**File**: `.processes/prompts/process-new.md`
**Action**: CLEAN UP — remove interactionOptions-specific text

Principle 8 covers the interaction options concept. Line 152 should just describe the approval flow without mentioning `interactionOptions` or `pendingInteraction`.

**Line 152**: Replace:
```
   - If next step has `approvalRequired: true`, **ensure `interactionOptions` are set** on the step entry in `process.json` (if missing, add default `approve`/`request-changes` options) and present them to the user alongside the deliverables, then wait
```
With:
```
   - If next step has `approvalRequired: true`, present deliverables to the user and wait for approval
```

---

### Item 7 — `create-step-file.json`

**File**: `.processes/steps/template/create-step-file/create-step-file.json`
**Action**: CLEAN UP

Remove the following entry from the `bestPractices` array (line 49):
```json
"ARCHITECTURAL BOUNDARY: Step definitions describe WHAT the step does. interactionOptions (user-facing choices like approve/request-changes) belong on the template/process level (template-definition.json steps[] and process.json steps[]), NOT in step definition JSON files. Steps may declare approvalRequired in their substeps, but the actual options shown to the user are defined by the template that references the step."
```

---

## Phase 4: Template Cleanup

### Item 8 — `update-process-template.json`

**File**: `.processes/templates/infrastructure/update-process-template/update-process-template.json`
**Action**: REMOVE

**8a. Remove from Step 2** (lines 55-58): Delete the `interactionOptions` property:
```json
"interactionOptions": [
  { "id": "approve", "label": "Approve", "description": "Approve the update plan and proceed to apply changes", "isDefault": true },
  { "id": "request-changes", "label": "Request Changes", "description": "Request modifications to the plan before approving" }
]
```

**8b. Remove from Step 4** (lines 72-75): Delete the `interactionOptions` property:
```json
"interactionOptions": [
  { "id": "approve", "label": "Approve", "description": "Approve the review and proceed", "isDefault": true },
  { "id": "request-changes", "label": "Request Changes", "description": "Request modifications before approving" }
]
```

---

## Phase 5: UI Updates (agentic-processes-ui repo)

### Item 9 — UI `src/types/index.ts`

**File**: `src/types/index.ts` (in `agentic-processes-ui`)
**Action**: RESTRUCTURE

**9a. Remove from ProcessStep** (lines 78-82): Remove:
```typescript
  /** Structured options for user selection when step needs input */
  interactionOptions?: InteractionOption[]
  /** IDs of options selected by the user (populated after selection) */
  selectedOptions?: string[]
```

**9b. Add PendingInteraction interface** (after `InteractionOption` interface, around line 67):
```typescript

/**
 * Represents a pending user interaction at the process level.
 * Set by the agent when user input is needed, cleared when user responds.
 */
export interface PendingInteraction {
  /** Options for user selection, dynamically generated by the agent */
  options: InteractionOption[]
}
```

**9c. Add to ProcessInstance** (after `steps: ProcessStep[]`, before `subProcessState`):
```typescript
  /** Current pending user interaction, if any */
  pendingInteraction?: PendingInteraction
```

---

### Item 10 — UI `src/services/lazyPromptsService.ts`

**File**: `src/services/lazyPromptsService.ts` (in `agentic-processes-ui`)
**Action**: UPDATE + RENAME

Rename `getActiveStepOptions` to `getInteractionOptions` (no longer step-level) and update body:
```typescript
// FROM:
export function getActiveStepOptions(process: ProcessInstance): InteractionOption[] | null {
  const activeStep = process.steps.find(s => s.id === process.currentState.activeStepId)
  return activeStep?.interactionOptions ?? null
}

// TO:
export function getInteractionOptions(process: ProcessInstance): InteractionOption[] | null {
  return process.pendingInteraction?.options ?? null
}
```

Also update the import + usage in `LazyPromptModal/index.tsx` (line 9 and 36):
- Line 9: `getActiveStepOptions` → `getInteractionOptions`
- Line 36: `getActiveStepOptions(process)` → `getInteractionOptions(process)`

---

### Item 11 — UI `src/components/LazyPromptModal/index.tsx`

**File**: `src/components/LazyPromptModal/index.tsx` (in `agentic-processes-ui`)
**Action**: UPDATE

**11a. Line 272**: Remove `isAlreadySelected` variable entirely:
```typescript
const isAlreadySelected = activeStep?.selectedOptions?.includes(item.option.id)
```

**11b.** Remove ALL `isAlreadySelected`-dependent rendering (check icon, opacity class, "selected" badge) throughout the component. `selectedOptions` no longer exists -- clean removal, no stubs.

---

### Item 12 — UI `src/components/DiagramView/StepNode.tsx`

**File**: `src/components/DiagramView/StepNode.tsx` (in `agentic-processes-ui`)
**Action**: REMOVE

Remove the "Options" badge entirely (lines 101-105):
```tsx
{step.interactionOptions && step.interactionOptions.length > 0 && (
  <span className="px-1.5 py-0.5 text-[10px] font-medium rounded bg-accent/20 text-accent">
    Options
  </span>
)}
```

---

## Phase 6: Process Migration

Remove `interactionOptions` (and `selectedOptions` where present) from step entries in existing `process.json` files. File 13 (this process) needs no changes.

### Item 13 — `process-update-process-template-20260204/process.json`

**File**: `.user-processes/active/process-update-process-template-20260204/process.json`
**Action**: MIGRATE

Remove from **Step 1** (lines 46-51):
```json
"interactionOptions": [
  { "id": "approve", "label": "Approve", "description": "Approve and proceed to next step", "isDefault": true },
  { "id": "request-changes", "label": "Request Changes", "description": "Request modifications before approving" },
  { "id": "reject", "label": "Reject", "description": "Reject this step entirely" }
],
"selectedOptions": ["approve"]
```

---

### Item 14 — `process-review-and-validate-updates-step-20260204/process.json`

**File**: `.user-processes/active/process-review-and-validate-updates-step-20260204/process.json`
**Action**: MIGRATE

Remove from **Step 1** (lines 42-46):
```json
"interactionOptions": [
  { "id": "approve", "label": "Approve", "description": "Approve and proceed to next step", "isDefault": true },
  { "id": "request-changes", "label": "Request Changes", "description": "Request modifications before approving" },
  { "id": "reject", "label": "Reject", "description": "Reject this step entirely" }
]
```

---

### Item 15 — `process-apply-template-updates-step-20260204/process.json`

**File**: `.user-processes/active/process-apply-template-updates-step-20260204/process.json`
**Action**: MIGRATE

Remove from **Step 1** (lines 42-46):
```json
"interactionOptions": [
  { "id": "approve", "label": "Approve", "description": "Approve and proceed to next step", "isDefault": true },
  { "id": "request-changes", "label": "Request Changes", "description": "Request modifications before approving" },
  { "id": "reject", "label": "Reject", "description": "Reject this step entirely" }
]
```

---

### Item 16 — `process-plan-template-updates-step-20260204/process.json`

**File**: `.user-processes/completed/process-plan-template-updates-step-20260204/process.json`
**Action**: MIGRATE

Remove from **Step 1** (lines 42-46):
```json
"interactionOptions": [
  { "id": "approve", "label": "Approve", "description": "Approve and proceed to next step", "isDefault": true },
  { "id": "request-changes", "label": "Request Changes", "description": "Request modifications before approving" },
  { "id": "reject", "label": "Reject", "description": "Reject this step entirely" }
]
```

Remove from **Step 2** (lines 55-58):
```json
"interactionOptions": [
  { "id": "approve", "label": "Approve", "description": "Approve created files and proceed to validation", "isDefault": true },
  { "id": "request-changes", "label": "Request Changes", "description": "Request modifications to the created files" }
]
```

---

### Item 17 — `process-analyze-existing-template-step-20260204/process.json`

**File**: `.user-processes/completed/process-analyze-existing-template-step-20260204/process.json`
**Action**: MIGRATE

Remove from **Step 1** (lines 42-46):
```json
"interactionOptions": [
  { "id": "approve", "label": "Approve", "description": "Approve and proceed to next step", "isDefault": true },
  { "id": "request-changes", "label": "Request Changes", "description": "Request modifications before approving" },
  { "id": "reject", "label": "Reject", "description": "Reject this step entirely" }
]
```

Remove from **Step 2** (lines 55-59):
```json
"interactionOptions": [
  { "id": "approve", "label": "Approve", "description": "Approve created files and proceed to validation", "isDefault": true },
  { "id": "request-changes", "label": "Request Changes", "description": "Request modifications to the created files" },
  { "id": "reject", "label": "Reject", "description": "Reject the created files entirely" }
]
```

---

## Execution Summary

| Phase | Items | Description | Repo |
|-------|-------|-------------|------|
| 1 | 1-2 | Framework types (foundation) | agentic-processes |
| 2 | 3-4 | Operating principle (concept) | agentic-processes |
| 3 | 5-7 | Guidance cleanup (remove scattered refs) | agentic-processes |
| 4 | 8 | Template cleanup | agentic-processes |
| 5 | 9-12 | UI updates | agentic-processes-ui |
| 6 | 13-17 | Process migration | agentic-processes |

**Dependencies**: Phase 1 must complete before Phase 5 (UI types mirror framework types). All other phases are independent of each other but ordered logically.
