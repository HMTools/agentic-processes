# Step 3: Detailed File Analysis

Analysis of exact current state for each target file, with specific line numbers and precise changes needed.

---

## 1. `.processes/steps/_components/operating-principles.md`

**Action**: ADD Principle 8

**Current State** (54 lines total):
- Lines 1-6: Header and intro
- Lines 7-11: Principle 1 (LOG FIRST, ACT SECOND)
- Lines 13-17: Principle 2 (READ JSON FOR GUIDANCE)
- Lines 19-23: Principle 3 (STOP AT CHECKPOINTS)
- Lines 25-29: Principle 4 (NO EXTERNAL TODOS)
- Lines 31-35: Principle 5 (VERIFY MANDATORY ACTIONS)
- Lines 37-42: Principle 6 (USE SUBAGENTS FOR STEPS)
- Lines 44-54: Principle 7 (FOLLOW TYPE STRUCTURES)
- Title says "The 7 Principles" (line 5)
- No mention of `interactionOptions` anywhere

**Precise Change**:
- Line 5: Change `## The 7 Principles` to `## The 8 Principles`
- After line 54 (end of file): Add new Principle 8 block:
```markdown

---

### 8. GENERATE INTERACTION OPTIONS
**Rule**: When you need user input (approval, choices, decisions), dynamically generate relevant options and set them in `process.json` `pendingInteraction` field. Never use predefined options from templates.

**Verification**: Output "✓ pendingInteraction set in process.json" when options are generated
```

---

## 2. `.processes/types/process-instance.ts`

**Action**: RESTRUCTURE

**Current State - ProcessStep (lines 113-146)**:
- Line 141-142: `/** Structured options for user selection when step needs input */` + `interactionOptions?: InteractionOption[];`
- Line 144-145: `/** IDs of options selected by the user (populated after selection) */` + `selectedOptions?: string[];`

**Current State - ProcessInstance (lines 41-68)**:
- Line 64: `steps: ProcessStep[];` — last field before subProcessState
- No `pendingInteraction` field

**Current State - InteractionOption (lines 163-175)**:
- Full interface definition — KEEP AS IS

**Precise Changes**:
1. **Remove from ProcessStep** (lines 140-145): Delete the following 6 lines:
   ```typescript
   
     /** Structured options for user selection when step needs input */
     interactionOptions?: InteractionOption[];
   
     /** IDs of options selected by the user (populated after selection) */
     selectedOptions?: string[];
   ```

2. **Add to ProcessInstance** (after line 64, before line 66 `subProcessState`): Add:
   ```typescript
   
     /** Current pending user interaction, if any. Set by agent when input is needed, cleared when user responds. */
     pendingInteraction?: PendingInteraction;
   ```

3. **Add new PendingInteraction interface** (after InteractionOption, before ParentProcessRef around line 176): Add:
   ```typescript
   
   /**
    * Represents a pending user interaction at the process level.
    * Set by the agent when user input is needed, cleared when user responds.
    */
   export interface PendingInteraction {
     /** Options for user selection, dynamically generated by the agent */
     options: InteractionOption[];
   }
   ```

4. **Update JSDoc example** (lines 147-161): Update the example to show `pendingInteraction` on process level instead of `interactionOptions` on step.

---

## 3. `.processes/types/template-definition.ts`

**Action**: REMOVE interactionOptions field

**Current State** (lines 71-81):
```typescript
    /** Structured options for user selection when step needs input */
    interactionOptions?: Array<{
      /** Unique identifier for this option */
      id: string;
      /** Display label for this option */
      label: string;
      /** Optional description/tooltip */
      description?: string;
      /** Whether this is the default option */
      isDefault?: boolean;
    }>;
```

**Precise Change**: Remove lines 71-81 (the entire `interactionOptions` field definition from the step type).

---

## 4. `.processes/steps/_components/mandatory-approval-checkpoint.md`

**Action**: CLEAN UP (remove interactionOptions-specific text)

**Current State** (30 lines):
- Line 7: `- [ ] **Ensure \`interactionOptions\` are set** on the current step entry in \`process.json\` — if user interaction is required and \`interactionOptions\` are missing, you MUST add them before presenting to the user`
- Line 8: `- [ ] Present the \`interactionOptions\` to the user (use the \`label\` and \`description\` fields)`
- Line 11: `- [ ] Record user's choice in \`selectedOptions\` on the step entry in \`process.json\``
- Lines 14-16: Two paragraphs about interactionOptions in process.json, explaining template/process level placement and "If interactionOptions are missing" fallback
- Line 26: `[Present interactionOptions from process.json here]`

**Precise Changes**:
1. **Line 7**: Replace with: `- [ ] **Generate interaction options** — dynamically create relevant options and set them in \`process.json\` \`pendingInteraction\` field (Principle 8)`
2. **Line 8**: Replace with: `- [ ] Present the options to the user (use the \`label\` and \`description\` fields from \`pendingInteraction.options\`)`
3. **Line 11**: Remove entirely (no `selectedOptions` anymore)
4. **Lines 14-16**: Replace both paragraphs with simpler text: `**Dynamic Options (Principle 8)**: When a step needs user input (approval, choices), the agent MUST dynamically generate options and set them in the \`pendingInteraction\` field on \`process.json\`. Options are never predefined in templates.`
5. **Line 26**: Replace with `[Present pendingInteraction options here]`

---

## 5. `.processes/prompts/process-new.md`

**Action**: CLEAN UP

**Current State** (line 152):
```
   - If next step has `approvalRequired: true`, **ensure `interactionOptions` are set** on the step entry in `process.json` (if missing, add default `approve`/`request-changes` options) and present them to the user alongside the deliverables, then wait
```

**Precise Change**: Replace line 152 with:
```
   - If next step has `approvalRequired: true`, dynamically generate interaction options and set `pendingInteraction` on `process.json` (Principle 8), present options to user alongside deliverables, then wait
```

---

## 6. `.processes/prompts/process-continue.md`

**Action**: ADD Principle 8 to inline list + CLEAN UP

**Current State - Inline Principles (lines 29-48)**:
- Lines 29-48: Seven principles listed inline (1 through 7)
- Line 48 ends with Principle 7 text about type structures

**Current State - interactionOptions text (line 177)**:
```
   - **Handle approval checkpoints**: If step has `approvalRequired: true`, the subagent will prepare deliverables and return; **ensure `interactionOptions` are set** on the step entry in `process.json` (if missing, add default `approve`/`request-changes` options) and present them to the user alongside the deliverables, then wait for approval
```

**Precise Changes**:
1. **After line 48** (after Principle 7): Add Principle 8 block:
   ```markdown
   
   8. **GENERATE INTERACTION OPTIONS** - When you need user input (approval, choices, decisions), dynamically generate options and set `pendingInteraction` in process.json
      - Output: "✓ pendingInteraction set in process.json"
   ```

2. **Line 177**: Replace with:
   ```
   - **Handle approval checkpoints**: If step has `approvalRequired: true`, the subagent will prepare deliverables and return; dynamically generate interaction options and set `pendingInteraction` on `process.json` (Principle 8), present options to user alongside deliverables, then wait for approval
   ```

---

## 7. `.processes/steps/template/create-step-file/create-step-file.json`

**Action**: CLEAN UP

**Current State** (line 49 in bestPractices array):
```json
"ARCHITECTURAL BOUNDARY: Step definitions describe WHAT the step does. interactionOptions (user-facing choices like approve/request-changes) belong on the template/process level (template-definition.json steps[] and process.json steps[]), NOT in step definition JSON files. Steps may declare approvalRequired in their substeps, but the actual options shown to the user are defined by the template that references the step."
```

**Precise Change**: Remove this entire bestPractices entry. The concept is now covered by Principle 8 (GENERATE INTERACTION OPTIONS) and interactionOptions no longer exists on templates.

---

## 8. `.processes/templates/infrastructure/update-process-template/update-process-template.json`

**Action**: REMOVE hardcoded interactionOptions

**Current State - Step 2 (lines 55-58)**:
```json
"interactionOptions": [
  { "id": "approve", "label": "Approve", "description": "Approve the update plan and proceed to apply changes", "isDefault": true },
  { "id": "request-changes", "label": "Request Changes", "description": "Request modifications to the plan before approving" }
]
```

**Current State - Step 4 (lines 72-75)**:
```json
"interactionOptions": [
  { "id": "approve", "label": "Approve", "description": "Approve the review and proceed", "isDefault": true },
  { "id": "request-changes", "label": "Request Changes", "description": "Request modifications before approving" }
]
```

**Precise Change**: Remove the `interactionOptions` property from both step 2 and step 4 objects.

---

## 9. UI `src/types/index.ts`

**Action**: RESTRUCTURE

**Current State - ProcessStep (lines 68-82)**:
- Line 79: `/** Structured options for user selection when step needs input */`
- Line 80: `interactionOptions?: InteractionOption[]`
- Line 81: `/** IDs of options selected by the user (populated after selection) */`
- Line 82 (inferred from content): `selectedOptions?: string[]`

**Current State - ProcessInstance (lines 106-116)**:
- Line 114: `steps: ProcessStep[]`
- Line 115: `subProcessState?: SubProcessState`
- No `pendingInteraction` field

**Current State - InteractionOption (lines 57-66)**:
- Full interface — KEEP AS IS

**Precise Changes**:
1. **Remove from ProcessStep** (lines 78-81): Remove:
   ```typescript
     /** Structured options for user selection when step needs input */
     interactionOptions?: InteractionOption[]
     /** IDs of options selected by the user (populated after selection) */
     selectedOptions?: string[]
   ```

2. **Add PendingInteraction interface** (after InteractionOption, around line 67): Add:
   ```typescript
   
   /**
    * Represents a pending user interaction at the process level.
    * Set by the agent when user input is needed, cleared when user responds.
    */
   export interface PendingInteraction {
     /** Options for user selection, dynamically generated by the agent */
     options: InteractionOption[]
   }
   ```

3. **Add to ProcessInstance** (after `steps`, before `subProcessState`): Add:
   ```typescript
     /** Current pending user interaction, if any */
     pendingInteraction?: PendingInteraction
   ```

---

## 10. UI `src/services/lazyPromptsService.ts`

**Action**: UPDATE

**Current State - getActiveStepOptions() (lines 29-32)**:
```typescript
export function getActiveStepOptions(process: ProcessInstance): InteractionOption[] | null {
  const activeStep = process.steps.find(s => s.id === process.currentState.activeStepId)
  return activeStep?.interactionOptions ?? null
}
```

**Precise Change**: Replace function body to read from process-level:
```typescript
export function getActiveStepOptions(process: ProcessInstance): InteractionOption[] | null {
  return process.pendingInteraction?.options ?? null
}
```

---

## 11. UI `src/components/LazyPromptModal/index.tsx`

**Action**: UPDATE

**Current State (line 36)**:
```typescript
const interactionOptions = useMemo(() => getActiveStepOptions(process), [process])
```
This already calls `getActiveStepOptions` which we're updating in the service — so this line is fine as-is.

**Current State (line 272)**:
```typescript
const isAlreadySelected = activeStep?.selectedOptions?.includes(item.option.id)
```
References `selectedOptions` which is being removed.

**Precise Changes**:
1. **Line 272**: Remove `isAlreadySelected` logic — replace with `const isAlreadySelected = false` or remove the concept entirely since `selectedOptions` no longer exists.
2. **Lines 285, 293-302, 324-327**: Remove all `isAlreadySelected`-dependent rendering (the check icon, the opacity class, the "selected" badge).

---

## 12. UI `src/components/DiagramView/StepNode.tsx`

**Action**: REMOVE Options badge

**Current State (lines 101-105)**:
```tsx
{step.interactionOptions && step.interactionOptions.length > 0 && (
  <span className="px-1.5 py-0.5 text-[10px] font-medium rounded bg-accent/20 text-accent">
    Options
  </span>
)}
```

**Precise Change**: Remove lines 101-105 entirely. The "Approval" badge (lines 96-99) already covers the indication that a step needs user interaction.

---

## 13-18. Process Migration Files

### 13. `process-set-dynamic-interaction-options-20260206/process.json`
**No interactionOptions found** on any step entries. No migration needed.

### 14. `process-update-process-template-20260204/process.json`
**Step 1** (lines 46-51): Has `interactionOptions` array (3 options) AND `selectedOptions: ["approve"]`
```json
"interactionOptions": [
  { "id": "approve", "label": "Approve", "description": "Approve and proceed to next step", "isDefault": true },
  { "id": "request-changes", "label": "Request Changes", "description": "Request modifications before approving" },
  { "id": "reject", "label": "Reject", "description": "Reject this step entirely" }
],
"selectedOptions": ["approve"]
```
**Action**: Remove both `interactionOptions` and `selectedOptions` from step 1.

### 15. `process-review-and-validate-updates-step-20260204/process.json`
**Step 1** (lines 42-46): Has `interactionOptions` array (3 options), no selectedOptions
```json
"interactionOptions": [
  { "id": "approve", "label": "Approve", "description": "Approve and proceed to next step", "isDefault": true },
  { "id": "request-changes", "label": "Request Changes", "description": "Request modifications before approving" },
  { "id": "reject", "label": "Reject", "description": "Reject this step entirely" }
]
```
**Action**: Remove `interactionOptions` from step 1.

### 16. `process-apply-template-updates-step-20260204/process.json`
**Step 1** (lines 42-46): Has `interactionOptions` array (3 options), no selectedOptions
```json
"interactionOptions": [
  { "id": "approve", "label": "Approve", "description": "Approve and proceed to next step", "isDefault": true },
  { "id": "request-changes", "label": "Request Changes", "description": "Request modifications before approving" },
  { "id": "reject", "label": "Reject", "description": "Reject this step entirely" }
]
```
**Action**: Remove `interactionOptions` from step 1.

### 17. `process-plan-template-updates-step-20260204/process.json`
**Step 1** (lines 42-46): Has `interactionOptions` array (3 options)
**Step 2** (lines 55-58): Has `interactionOptions` array (2 options)
```json
// Step 1
"interactionOptions": [
  { "id": "approve", "label": "Approve", "description": "Approve and proceed to next step", "isDefault": true },
  { "id": "request-changes", "label": "Request Changes", "description": "Request modifications before approving" },
  { "id": "reject", "label": "Reject", "description": "Reject this step entirely" }
]
// Step 2
"interactionOptions": [
  { "id": "approve", "label": "Approve", "description": "Approve created files and proceed to validation", "isDefault": true },
  { "id": "request-changes", "label": "Request Changes", "description": "Request modifications to the created files" }
]
```
**Action**: Remove `interactionOptions` from steps 1 and 2.

### 18. `process-analyze-existing-template-step-20260204/process.json`
**Step 1** (lines 42-46): Has `interactionOptions` array (3 options)
**Step 2** (lines 55-59): Has `interactionOptions` array (3 options)
```json
// Step 1
"interactionOptions": [
  { "id": "approve", "label": "Approve", "description": "Approve and proceed to next step", "isDefault": true },
  { "id": "request-changes", "label": "Request Changes", "description": "Request modifications before approving" },
  { "id": "reject", "label": "Reject", "description": "Reject this step entirely" }
]
// Step 2
"interactionOptions": [
  { "id": "approve", "label": "Approve", "description": "Approve created files and proceed to validation", "isDefault": true },
  { "id": "request-changes", "label": "Request Changes", "description": "Request modifications to the created files" },
  { "id": "reject", "label": "Reject", "description": "Reject the created files entirely" }
]
```
**Action**: Remove `interactionOptions` from steps 1 and 2.

---

## Summary of Changes by Type

| Change Type | Count | Files |
|-------------|-------|-------|
| ADD new content | 2 | operating-principles.md, process-continue.md (inline principles) |
| RESTRUCTURE types | 2 | process-instance.ts, UI types/index.ts |
| REMOVE fields from types | 1 | template-definition.ts |
| CLEAN UP guidance | 4 | mandatory-approval-checkpoint.md, process-new.md, process-continue.md, create-step-file.json |
| REMOVE template data | 1 | update-process-template.json |
| UPDATE UI logic | 2 | lazyPromptsService.ts, LazyPromptModal/index.tsx |
| REMOVE UI element | 1 | StepNode.tsx |
| MIGRATE process data | 5 | 5 process.json files (1 has no changes needed) |
